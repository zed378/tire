node_modules/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo

# Kredensial — tidak pernah masuk repo (PLAN/09 §4.5, PLAN/13 §8)
.env
.env.*
!.env.example

# Artefak lokal
.DS_Store
*.log
apps/api/prisma/*.db
playwright-report/
test-results/
.stryker-tmp/

# Local photo storage (STORAGE_DRIVER=local). The volume is the evidence;
# it belongs in the backup job, not in the repository.
apps/api/uploads

# Photographic originals. Several MB each, nothing serves them; the
# derivatives in public/img/ are what ships. Refetch with `pnpm --filter
# @c26/web images:fetch`.
apps/web/public/img/source/
